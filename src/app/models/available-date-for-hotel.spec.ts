import { AvailableDateForHotel } from './available-date-for-hotel';

describe('AvailableDateForHotel', () => {
  it('should create an instance', () => {
    expect(new AvailableDateForHotel()).toBeTruthy();
  });
});
